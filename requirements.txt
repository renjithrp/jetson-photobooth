# --- core (required) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-multipart==0.0.9
aiofiles==23.2.1
Pillow==10.3.0
qrcode==7.4.2
httpx==0.27.0           # guest captive-portal (:80) proxy to the HTTPS backend
numpy>=1.26             # face clustering (numpy 2 OK; rembg/insightface pull it in)
pyserial==3.5           # USB Arduino Nano button trigger

# --- optional (install on the Pi as needed) ---
# opencv-python-headless      # webcam live view + gesture frames
# mediapipe                   # hand-gesture trigger (aarch64 wheel may need a custom build)
# gpiod                       # GPIO button trigger (or: apt install python3-libgpiod)
# rclone is a SYSTEM binary for Google Drive (apt/install script), not a pip package
